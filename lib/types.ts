export type Product = {
    id: number;
    title: string,
    company: string,
    description: string,
    price: number,
    price_unit: string,
    discount: number,
    discount_unit: string,
    available: number,
    images: string[]
}